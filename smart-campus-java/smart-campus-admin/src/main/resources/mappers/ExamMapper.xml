<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smart.campus.admin.mappers.ExamMapper">

    <resultMap id="BaseResultMap" type="com.smart.campus.admin.entity.Exam">
        <id column="id" property="id" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
        <result column="name" property="name" />
        <result column="course_id" property="courseId" />
        <result column="paper_id" property="paperId" />
        <result column="exam_date" property="examDate" />
        <result column="start_time" property="startTime" />
        <result column="duration" property="duration" />
        <result column="location" property="location" />
        <result column="invigilator" property="invigilator" />
        <result column="total_students" property="totalStudents" />
        <result column="attended_students" property="attendedStudents" />
        <result column="status" property="status" />
        <result column="remark" property="remark" />
    </resultMap>

    <sql id="BaseColumns">
        id, create_time, update_time, name, course_id, paper_id, exam_date, start_time,
        duration, location, invigilator, total_students, attended_students, status, remark
    </sql>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns" /> FROM biz_exam WHERE id = #{id}
    </select>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns" /> FROM biz_exam
        WHERE 1=1
        <if test="keyword != null and keyword != ''">
            AND name LIKE CONCAT('%', #{keyword}, '%')
        </if>
        <if test="status != null and status != ''">
            AND status = #{status,jdbcType=INTEGER}
        </if>
        <if test="startDate != null and startDate != ''">
            AND exam_date >= #{startDate}
        </if>
        <if test="endDate != null and endDate != ''">
            AND exam_date &lt;= #{endDate}
        </if>
        ORDER BY create_time DESC
        <if test="pageNo != null and pageSize != null">
            LIMIT #{pageNo}, #{pageSize}
        </if>
    </select>

    <select id="selectCount" resultType="long">
        SELECT COUNT(*) FROM biz_exam
        WHERE 1=1
        <if test="keyword != null and keyword != ''">
            AND name LIKE CONCAT('%', #{keyword}, '%')
        </if>
        <if test="status != null and status != ''">
            AND status = #{status,jdbcType=INTEGER}
        </if>
        <if test="startDate != null and startDate != ''">
            AND exam_date >= #{startDate}
        </if>
        <if test="endDate != null and endDate != ''">
            AND exam_date &lt;= #{endDate}
        </if>
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO biz_exam (name, course_id, paper_id, exam_date, start_time, duration,
                              location, invigilator, total_students, attended_students,
                              status, remark, create_time, update_time)
        VALUES (#{name}, #{courseId}, #{paperId}, #{examDate}, #{startTime}, #{duration},
                #{location}, #{invigilator}, #{totalStudents}, #{attendedStudents},
                #{status}, #{remark}, NOW(), NOW())
    </insert>

    <update id="update">
        UPDATE biz_exam
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="courseId != null">course_id = #{courseId},</if>
            <if test="paperId != null">paper_id = #{paperId},</if>
            <if test="examDate != null">exam_date = #{examDate},</if>
            <if test="startTime != null">start_time = #{startTime},</if>
            <if test="duration != null">duration = #{duration},</if>
            <if test="location != null">location = #{location},</if>
            <if test="invigilator != null">invigilator = #{invigilator},</if>
            <if test="totalStudents != null">total_students = #{totalStudents},</if>
            <if test="attendedStudents != null">attended_students = #{attendedStudents},</if>
            <if test="status != null">status = #{status},</if>
            <if test="remark != null">remark = #{remark},</if>
            update_time = NOW()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM biz_exam WHERE id = #{id}
    </delete>

    <delete id="deleteByIds">
        DELETE FROM biz_exam WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

</mapper>
