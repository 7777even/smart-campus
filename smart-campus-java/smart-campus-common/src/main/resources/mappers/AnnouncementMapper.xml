<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campus.mappers.AnnouncementMapper">

    <resultMap id="BaseResultMap" type="com.campus.entity.Announcement">
        <id column="id" property="id" />
        <result column="title" property="title" />
        <result column="content" property="content" />
        <result column="publisher" property="publisher" />
        <result column="level" property="level" />
        <result column="status" property="status" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <sql id="Base_Column_List">
        id, title, content, publisher, level, status, create_time, update_time
    </sql>

    <sql id="Where_Clause">
        <where>
            <if test="keyword != null and keyword != ''">
                AND title LIKE CONCAT('%', #{keyword}, '%')
            </if>
            <if test="level != null and level != ''">
                AND level = #{level}
            </if>
            <if test="status != null and status != ''">
                AND status = #{status,jdbcType=INTEGER}
            </if>
        </where>
    </sql>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List" />
        FROM biz_announcement WHERE id = #{id}
    </select>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List" />
        FROM biz_announcement
        <include refid="Where_Clause" />
        ORDER BY create_time DESC
        <if test="pageNo != null and pageSize != null">
            LIMIT #{pageNo}, #{pageSize}
        </if>
    </select>

    <select id="selectCount" resultType="long">
        SELECT COUNT(*) FROM biz_announcement
        <include refid="Where_Clause" />
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO biz_announcement (title, content, publisher, level, status, create_time, update_time)
        VALUES (#{title}, #{content}, #{publisher}, #{level}, #{status}, NOW(), NOW())
    </insert>

    <update id="update">
        UPDATE biz_announcement
        SET title = #{title},
            content = #{content},
            publisher = #{publisher},
            level = #{level},
            status = #{status},
            update_time = NOW()
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM biz_announcement WHERE id = #{id}
    </delete>

    <delete id="deleteByIds">
        DELETE FROM biz_announcement WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

</mapper>
