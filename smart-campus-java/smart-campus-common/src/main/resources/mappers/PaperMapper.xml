<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campus.mappers.PaperMapper">

    <resultMap id="BaseResultMap" type="com.campus.entity.Paper">
        <id column="id" property="id" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
        <result column="name" property="name" />
        <result column="course_id" property="courseId" />
        <result column="total_score" property="totalScore" />
        <result column="pass_score" property="passScore" />
        <result column="duration" property="duration" />
        <result column="single_count" property="singleCount" />
        <result column="single_score" property="singleScore" />
        <result column="multi_count" property="multiCount" />
        <result column="multi_score" property="multiScore" />
        <result column="judge_count" property="judgeCount" />
        <result column="judge_score" property="judgeScore" />
        <result column="question_count" property="questionCount" />
        <result column="status" property="status" />
        <result column="description" property="description" />
    </resultMap>

    <sql id="BaseColumns">
        id, create_time, update_time, name, course_id, total_score, pass_score,
        duration, single_count, single_score, multi_count, multi_score,
        judge_count, judge_score, question_count, status, description
    </sql>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns" /> FROM biz_paper WHERE id = #{id}
    </select>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns" /> FROM biz_paper
        WHERE 1=1
        <if test="keyword != null and keyword != ''">
            AND name LIKE CONCAT('%', #{keyword}, '%')
        </if>
        <if test="courseId != null">
            AND course_id = #{courseId}
        </if>
        <if test="status != null and status != ''">
            AND status = #{status,jdbcType=INTEGER}
        </if>
        ORDER BY create_time DESC
        <if test="pageNo != null and pageSize != null">
            LIMIT #{pageNo}, #{pageSize}
        </if>
    </select>

    <select id="selectCount" resultType="long">
        SELECT COUNT(*) FROM biz_paper
        WHERE 1=1
        <if test="keyword != null and keyword != ''">
            AND name LIKE CONCAT('%', #{keyword}, '%')
        </if>
        <if test="courseId != null">
            AND course_id = #{courseId}
        </if>
        <if test="status != null and status != ''">
            AND status = #{status,jdbcType=INTEGER}
        </if>
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO biz_paper (name, course_id, total_score, pass_score, duration,
                               single_count, single_score, multi_count, multi_score,
                               judge_count, judge_score, question_count, status, description,
                               create_time, update_time)
        VALUES (#{name}, #{courseId}, #{totalScore}, #{passScore}, #{duration},
                #{singleCount}, #{singleScore}, #{multiCount}, #{multiScore},
                #{judgeCount}, #{judgeScore}, #{questionCount}, #{status}, #{description},
                NOW(), NOW())
    </insert>

    <update id="update">
        UPDATE biz_paper
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="courseId != null">course_id = #{courseId},</if>
            <if test="totalScore != null">total_score = #{totalScore},</if>
            <if test="passScore != null">pass_score = #{passScore},</if>
            <if test="duration != null">duration = #{duration},</if>
            <if test="singleCount != null">single_count = #{singleCount},</if>
            <if test="singleScore != null">single_score = #{singleScore},</if>
            <if test="multiCount != null">multi_count = #{multiCount},</if>
            <if test="multiScore != null">multi_score = #{multiScore},</if>
            <if test="judgeCount != null">judge_count = #{judgeCount},</if>
            <if test="judgeScore != null">judge_score = #{judgeScore},</if>
            <if test="questionCount != null">question_count = #{questionCount},</if>
            <if test="status != null">status = #{status},</if>
            <if test="description != null">description = #{description},</if>
            update_time = NOW()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM biz_paper WHERE id = #{id}
    </delete>

    <delete id="deleteByIds">
        DELETE FROM biz_paper WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

</mapper>
