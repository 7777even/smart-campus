<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campus.mappers.AiStudentProfileMapper">

    <resultMap id="BaseResultMap" type="com.campus.entity.AiStudentProfile">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="student_id" property="studentId" jdbcType="BIGINT"/>
        <result column="student_no" property="studentNo" jdbcType="VARCHAR"/>
        <result column="student_name" property="studentName" jdbcType="VARCHAR"/>
        <result column="class_id" property="classId" jdbcType="BIGINT"/>
        <result column="major_id" property="majorId" jdbcType="BIGINT"/>
        <result column="department_id" property="departmentId" jdbcType="BIGINT"/>
        <result column="gpa" property="gpa" jdbcType="DECIMAL"/>
        <result column="attendance_rate" property="attendanceRate" jdbcType="DECIMAL"/>
        <result column="homework_avg" property="homeworkAvg" jdbcType="DECIMAL"/>
        <result column="exam_avg" property="examAvg" jdbcType="DECIMAL"/>
        <result column="comprehensive_score" property="comprehensiveScore" jdbcType="DECIMAL"/>
        <result column="risk_level" property="riskLevel" jdbcType="VARCHAR"/>
        <result column="trend" property="trend" jdbcType="VARCHAR"/>
        <result column="last_calc_time" property="lastCalcTime" jdbcType="TIMESTAMP"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="BaseColumns">
        id, student_id, student_no, student_name, class_id, major_id, department_id,
        gpa, attendance_rate, homework_avg, exam_avg, comprehensive_score,
        risk_level, trend, last_calc_time, create_time, update_time
    </sql>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/> FROM ai_student_profile WHERE id = #{id}
    </select>

    <select id="selectByStudentId" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/> FROM ai_student_profile WHERE student_id = #{studentId}
    </select>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/> FROM ai_student_profile
        WHERE 1=1
        <if test="riskLevel != null and riskLevel != ''">
            AND risk_level = #{riskLevel}
        </if>
        <if test="departmentId != null">
            AND department_id = #{departmentId}
        </if>
        <if test="keyword != null and keyword != ''">
            AND (student_name LIKE CONCAT('%', #{keyword}, '%') OR student_no LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        ORDER BY comprehensive_score ASC
        LIMIT #{pageSize} OFFSET #{pageNo}
    </select>

    <select id="selectCount" resultType="long">
        SELECT COUNT(*) FROM ai_student_profile
        WHERE 1=1
        <if test="riskLevel != null and riskLevel != ''">
            AND risk_level = #{riskLevel}
        </if>
        <if test="departmentId != null">
            AND department_id = #{departmentId}
        </if>
        <if test="keyword != null and keyword != ''">
            AND (student_name LIKE CONCAT('%', #{keyword}, '%') OR student_no LIKE CONCAT('%', #{keyword}, '%'))
        </if>
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ai_student_profile (student_id, student_no, student_name, class_id, major_id, department_id,
            gpa, attendance_rate, homework_avg, exam_avg, comprehensive_score,
            risk_level, trend, last_calc_time, create_time, update_time)
        VALUES (#{studentId}, #{studentNo}, #{studentName}, #{classId}, #{majorId}, #{departmentId},
            #{gpa}, #{attendanceRate}, #{homeworkAvg}, #{examAvg}, #{comprehensiveScore},
            #{riskLevel}, #{trend}, #{lastCalcTime}, NOW(), NOW())
    </insert>

    <update id="update">
        UPDATE ai_student_profile
        <set>
            <if test="gpa != null">gpa = #{gpa},</if>
            <if test="attendanceRate != null">attendance_rate = #{attendanceRate},</if>
            <if test="homeworkAvg != null">homework_avg = #{homeworkAvg},</if>
            <if test="examAvg != null">exam_avg = #{examAvg},</if>
            <if test="comprehensiveScore != null">comprehensive_score = #{comprehensiveScore},</if>
            <if test="riskLevel != null">risk_level = #{riskLevel},</if>
            <if test="trend != null">trend = #{trend},</if>
            <if test="lastCalcTime != null">last_calc_time = #{lastCalcTime},</if>
            update_time = NOW()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM ai_student_profile WHERE id = #{id}
    </delete>

    <delete id="deleteByIds">
        DELETE FROM ai_student_profile WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <delete id="deleteByStudentId">
        DELETE FROM ai_student_profile WHERE student_id = #{studentId}
    </delete>

</mapper>
